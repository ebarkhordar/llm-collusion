# Project configuration
paths:
  data_dir: data
  output_path: data/code_pairs.jsonl

benchmark:
  source: mbpp
  num_tasks: 5

models:
  - openai/gpt-5
  - anthropic/claude-haiku-4.5

api:
  concurrency: 6
